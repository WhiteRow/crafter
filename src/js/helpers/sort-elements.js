function SortElements(Elements, action) {
	if (typeof Items === "object") {
		Items.forEach(item => {
			item.action;
		});
	} else {
		console.error("Element must be an object");
	}
}

export default SortElements;
